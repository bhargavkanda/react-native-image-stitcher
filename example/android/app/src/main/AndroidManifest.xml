<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--
      Camera — iOS counterpart: NSCameraUsageDescription in Info.plist.
      Required by react-native-vision-camera v4 at runtime.  Without
      this declaration, ActivityCompat.requestPermissions auto-denies
      the camera permission WITHOUT showing a system dialog (Android
      treats undeclared permissions as "not requestable"), and any
      "Grant Permission" UI the SDK shows will do nothing.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!--
      ARCore high-rate sensor access — required since Android 12 (API 31).
      ARCore polls accelerometer + gyroscope at >= 200 Hz to fuse pose;
      Android 12 rate-limits / denies sensor streams above 200 Hz unless
      the app declares this normal permission.  Without it,
      Session.resume() throws a generic FatalException sourced from
      `android_sensors.cc` deep in ARCore's native code, and the AR
      capture path never starts.

      Normal permission (protectionLevel="normal"): auto-granted at
      install, no runtime prompt needed.
    -->
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!--
        ARCore meta-data — required by the ARCore SDK (com.google.ar:core).
        Without this tag, ArCoreApk.requestInstall() throws
        FatalException("Application manifest must contain meta-data
        com.google.ar.core") on the FIRST call, even before any AR
        functionality runs.  Blocks the AR capture path entirely.

        Value `optional` (not `required`):
          - The app remains installable on devices without ARCore support
            (Play Store doesn't filter).  Devices without ARCore fall
            back to the vision-camera + gyro non-AR capture path.
          - On ARCore-supported devices the AR preview path works as
            the primary capture flow.
      -->
      <meta-data android:name="com.google.ar.core" android:value="optional" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:screenOrientation="portrait"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
