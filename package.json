{
  "name": "@retailens/capture-sdk",
  "version": "0.0.1",
  "description": "RetaiLens Capture SDK — camera capture, quality checks, and video stitching for retail shelf monitoring",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "retailens",
    "capture",
    "camera",
    "react-native",
    "sdk"
  ],
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "peerDependencies": {
    "react-native": ">=0.72.0"
  },
  "peerDependenciesMeta": {
    "react-native": {
      "optional": true
    }
  }
}
