{
  "name": "@retailens/capture-sdk",
  "version": "0.1.0",
  "description": "RetaiLens Capture SDK — camera capture, quality checks, and video stitching for retail shelf monitoring",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "retailens",
    "capture",
    "camera",
    "react-native",
    "sdk"
  ],
  "license": "UNLICENSED",
  "private": true,
  "//": "DO NOT add react-native / react-native-vision-camera as devDependencies. Host apps provide them via peerDependencies; having nested copies here causes duplicate-TurboModule errors in Jest.",
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.5.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-native": ">=0.72.0",
    "react-native-vision-camera": ">=4.0.0",
    "react-native-sensors": ">=7.0.0",
    "expo-sensors": ">=14.0.0",
    "react-native-safe-area-context": ">=4.0.0"
  }
}
