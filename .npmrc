# Allow npm install to proceed when peer dependency versions don't
# perfectly satisfy our peerDependencies — useful when consumers pin
# specific minors of react / react-native / vision-camera that don't
# exactly match the ">=" we declare.  Without this, npm 7+ refuses to
# install on peer-dep mismatch by default, blocking adoption.
legacy-peer-deps=true
